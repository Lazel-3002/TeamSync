# Roblox game rules

- `server/ServerScriptService/DistortionSilahi.server.luau` is a protected, user-approved asset.
- Do not delete, rename, replace, disable, or remove the Distortion weapon or its generated `DistortionSilahi` model unless the user explicitly revokes this requirement.
- Preserve the animated cyan energy cores and rotating crystal fins.
- Preserve the requirement that the cores do not use `PointLight`, `SpotLight`, or `SurfaceLight` and do not illuminate the surrounding environment.
- Future Roblox work must keep this weapon present while adding or changing other content.
